
import 'package:flutter/material.dart';

class SoftSkillsPage extends StatelessWidget {
  const SoftSkillsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(child: Text('You are here')),
    );
  }
}